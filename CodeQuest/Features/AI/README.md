# AI assistant

The AI assistant is a Premium feature. Send the current course, module and
chapter as context to the backend, never expose the DeepSeek API key in the
browser, and add conversation history tables before storing chat messages.
